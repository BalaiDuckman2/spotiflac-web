[project]
name = "spotiflac-web"
version = "0.1.0"
description = "Self-hosted web UI for SpotiFLAC"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
